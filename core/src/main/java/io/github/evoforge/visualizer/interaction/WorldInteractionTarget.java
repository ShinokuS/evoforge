package io.github.evoforge.visualizer.interaction;

/** One currently visible/walkable world cell resolved from the active presentation mode. */
record WorldInteractionTarget(int x, int y, int z) { }
