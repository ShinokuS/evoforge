package io.github.evoforge.visualizer.presentation.object;

/** Small presentation-only renderer families; simulation never observes these values. */
public enum ObjectVisualFamily {
    GENERIC,
    CREATURE,
    VEGETATION
}
