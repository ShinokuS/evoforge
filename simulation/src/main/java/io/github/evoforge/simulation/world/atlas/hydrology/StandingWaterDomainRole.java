package io.github.evoforge.simulation.world.atlas.hydrology;

/** Hydrologic standing-water domain role derived from world topology, not body scale. */
public enum StandingWaterDomainRole {
    OCEANIC,
    INLAND
}
